# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# venv
.venv/
venv/
env/

# uv
.python-version

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Model weights — never in git
*.pt
*.pth
*.ckpt
*.onnx
*.safetensors
models/

# Experiment output. Sweeps produce thousands of runs; results are regenerated
# from the config + seed, so the artifacts do not belong in history. Summary
# tables and figures that go in the paper are committed EXPLICITLY, by path.
runs/
sweeps/
outputs/
logs/
wandb/
cache/importance/
*.rrd

# Notion / Obsidian sync artifacts
.obsidian/

# Local AI agent config — private, not part of the public repo
CLAUDE.md

# Working notes — kept out of the tree while the work is in progress.
docs/private/

# Personal study aids — line-by-line explanations, not shipped
explanations/
